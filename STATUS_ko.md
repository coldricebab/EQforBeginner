*[English](STATUS.md) · 한국어*

# 구현 현황

최종 갱신: 2026-07-29.

범례: **구현됨**은 결정론적 검사를 통과하는 실제 코드가 있다는 뜻이다. **부분 구현**은 사용
가능한 일부가 있고 빠진 경계가 명시되어 있다는 뜻이다. **미구현**은 목(mock) 뒤에 숨긴 것이
아니라 의도적으로 없는 것이다.

## 단계별 현황

| 단계 | 상태 | 무엇이 있고, 어디서 멈추는가 |
| --- | --- | --- |
| 0 — 골격 | 구현됨 | Tauri 2 / React / TypeScript 셸, Rust 워크스페이스, DSP 라이브러리, CLI 하네스, CI 정의. macOS `.app` 개발자 번들이 로컬에서 빌드된다. 호스팅 CI는 아직 한 번도 실행되지 않았다. |
| 1 — 오프라인 DSP 코어 | 부분 구현 | 결정론적 합성 6지점 fixture → 분석 → 타깃 → 공간 게이트를 거친 `[-12,+3] dB` minimum-phase 스테레오 FIR → 예측 검증 → float32 WAV/ZIP. 임의의 사용자 WAV/CSV 프로젝트 임포트는 미구현이다. |
| 2 — 측정 마법사 | 부분 구현, 캡처 경로 동작 | CPAL로 네이티브 채널을 명시 선택하는 제한된 48 kHz 입력 전용 캡처를 수행하며, 앱은 출력 장치를 절대 열지 않는다. 마법사는 대역을 포괄하는 UMIK 캘리브레이션과 좌우 스윕 WAV를 임포트하고, 메인/시작/종료 채널 라우팅을 보고하며, 스윕 레벨을 계측하고, 변경 불가능한 원본 WAV/JSON 증거를 영속화한다. `known-marker-capture-endpoint-v4`는 잔향으로 크기가 다른 마커 후보를 허용하면서도 엄격한 쌍 간격 검증은 유지한다. `known-sweep-deconvolution-v5`는 피크 정렬 없이 부호 있는 4,800 샘플 pre-zero 창을 유지하고, 32,768 샘플(0.68 s)을 보존해 65,536점 FFT(0.73 Hz 격자)로 분석하므로 좁은 저역 모드가 더 이상 빈 사이에서 과소평가되지 않는다. v3 이전 캐시는 복원되지 않는다. 인증된 SPL, 하드웨어 볼륨 제어, 음향적 좌우 뒤바뀜 감지, 과도 신호·마이크 이동 분류는 없다. |
| 3 — 단일 서브 통합 | 부분 구현, 실측 탐색 구현 | 2.1 프로젝트는 물리적으로 설정한 2–12개 크로스오버 상태를 기록하고, 승인된 P0 메인 단독·서브 단독 캡처를 안내한다. 코어는 캘리브레이션된 복소 경로로부터 제한된 메인 지연 격자와 양쪽 극성을 합성한다. 크로스오버 전달함수를 지어내지 않고 서브 레벨을 탐색하지도 않는다. 랭킹은 명시적으로 예측 전용이며, 승자를 실제 하드웨어에 입력한 뒤에야 새 결합 캡처로 다음 단계가 열린다. 서브 레벨 탐색, 위상 노브 탐색, 다지점 후보 채점은 없다. |
| 4 — 폐루프 검증 | 부분 구현, 실측 게이트 구현 | 실측 어댑터는 제한 게인 / minimum-phase / 보호된 딥 / 공간 설계를 그대로 재사용하며, B&K 계열·Harman 계열·임포트한 사용자 타깃을 지원한다. 예측 전용 시험 ZIP을 생성하고, 사용자가 그 시험 필터를 Roon에서 활성으로 선언한 뒤 새로 승인된 P0 좌우 캡처를 요구한다. `live-closed-loop-validation-v4`는 1/12옥타브 스무딩된 커브에서 옥타브 셀별 RMSE의 RMS로 개선을 판정하고, 20–650 Hz 구간에서 예측 대 검증 일치를 3 dB 이하로 요구하며, 적용 배율을 추정해 필터가 걸리지 않았거나 두 번 걸린 경우를 잡아내고, 스무딩하지 않은 잔차를 진단으로 보존한다. 실패하면 그대로 내보내지 않고 재설계와 재검증을 안내한다. 좌우 뒤바뀜 자동 감지는 없다. |
| 5 — Roon 패키지 | 부분 구현, 검증된 실측 내보내기 구현 | 폐루프를 통과한 프로젝트는 네이티브 샘플레이트 재설계를 실행하고, 최종 48 kHz 멤버를 검증된 시험 필터에 응답으로 묶고(0.05 dB / 0.02 ms), 6개 레이트 minimum-phase Roon ZIP을 쓰고 되읽으며, 최종 프로젝트 기록과 해시를 남기고 제한된 헤드룸을 계산한다. 시험·최종 단계는 현재 세션의 패키지만 재검증하고 바이트 단위로 확인하는 네이티브 저장 대화상자를 제공한다. Windows 실행, 설치 파일 검증, Developer ID 서명, 공증은 없다. |

실험적 **SECS 고급 옵션**(`secs-port-v1`) — 한플님이 만드신 SECS를 원작자가
부여한 MIT 라이선스로 이식한 단일 지점 전대역 알고리즘([원본 글](https://gall.dcinside.com/mgallery/board/view/?id=speakers&no=514096&s_type=search_name&s_keyword=%ED%95%9C%ED%94%8C&page=1),
귀속은 `THIRD-PARTY-NOTICES.md`) — 은 승인된 중앙 P0 쌍만으로 전대역 혼합위상 시험본을
설계하며, 다좌석 안전 설계를 의도적으로 우회한다. SECS.py의 조절값 전체
(부스트 한계, 틸트, 베이스 셸프, 해상도, 커튼, 레이턴시 모드, 고정/자동 지연)가
고급 옵션에 노출되고 백엔드가 범위를 검증하며, 사용된 설정 그대로 최종
내보내기에서 재사용된다. 다지점 옵션(기본 켬, SECS.py에 없는 확장)은 크기
보정에 승인된 모든 베이스라인 좌석의 가중 RMS 평균 — Phase 4 설계와 같은
공간 통계 — 을 공급하되, 초과위상 보정과 그 신뢰도 가드는 P0 쌍에 엄격히
유지한다. P0만 승인된 경우 자동으로 단일 지점 경로로 돌아간다.
타겟 커브 옵션(또 하나의 확장, UI 기본 켬)은 앱에서 선택한 타겟 커브 —
B&K-style, Harman-style, 또는 가져온 커스텀 커브를 500 Hz 0 dB로 재정박 —
를 SECS 적응 타겟에 겹쳐, Phase 4 설계와 같은 하우스 커브를 목표로 보정한다.
끄면 SECS 원본의 실내 평탄 타겟을 재현하며, 최종 내보내기는 시험본이 실제로
사용한 커브 사본을 재사용한다. 2.1 프로젝트도 지원한다: 베이스 매니지먼트는 선형이라
채널별 결합(메인+서브) 측정을 독립 보정하는 것은 측정 지점에서 정확하다.
확정된 크로스오버 아래 — 공유 서브 하나가 두 채널을 재생해서 측정된 L/R
차이가 같은 경로의 노이즈인 대역(첫 실기 2.1 패키지에서 37 Hz에 11.8 dB
허위 분리 실측) — 에서는 크기와 초과위상 보정을 L/R 공통화하고 크로스오버
위 반옥타브에 걸쳐 페이드아웃한다. Phase 4의 공통 저역 정규화에 대응하는
SECS 버전이며, 베이스 매니지먼트가 두 채널을 서브로 합산하는 지점에서 두
필터의 위상도 일치시킨다. 폐루프 규율은 기본 경로와 동일하다: 시험본은 예측 전용이고, Roon 활성 선언 후 새 P0
L/R 캡처가 스무딩된 20–650 Hz 판정(개선 또는 예측 달성, 3 dB 일치, 적용 스케일
[0.6, 1.4], 세션 게인 게이트 — Phase 4의 12.05 dB 감쇠 한계는 SECS 컷이
설계상 무제한이므로 문서화 후 적용하지 않으며, +3 dB 양의 이득 게이트는
피크 정규화 보호를 위해 유지)을 통과해야 한다. 고역은 판정에서 제외한다 —
센티미터 수준의 마이크 재배치가 그 대역을 지배하기 때문이다. 최종 내보내기는
scipy `resample_poly` 패리티 리샘플러로 P0 임펄스를 각 네이티브 레이트로
리샘플해 SECS를 레이트별로 재실행하고, 검증된 지연을 고정하며, 48 kHz 멤버를
검증된 시험 WAV에 바이트 단위로 바인딩하고, 나머지 레이트의 레벨을 거기에
정렬(레이트별 기록, 헤드룸에 반영)하며, 레이트별 스무딩 일치 진단을 기록한다.
SECS 헤드룸은 v3 기반에 프로그램 소재 피크 성장 기준(풀스케일 사각파·킥·
클리핑 노이즈 대용 신호를 모든 레이트 멤버에 컨볼브)을 추가한다 — 첫 실기
패키지에서 스윕 기반이 약 5 dB 과소평가되어 재생이 클리핑됐기 때문이다.
검증이 통과하면 Phase 4 경로와 같은 실측 전/후 차트를 렌더하고, 검증
전에는 설계 시점의 예측 전용 차트(raw/타겟/예측 — verified 곡선은
가짜로 그리지 않고 숨김)를 렌더한다. 사용자는
검증을 명시적으로 건너뛰고 예측 전용 패키지를 내보낼 수 있으며, 파일명·
README·프로젝트 기록에 예측 전용으로 표기되고 검증된 것으로 제시되지
않는다. Python 원본과의 수치 패리티는 `crates/dsp-core/tests/secs_parity.rs`가
`testdata/secs-parity.json`으로 고정한다. 2026-07-30 첫 실기 마이크/Roon
실행에서 SECS 폐루프가 통과하고 검증된 패키지가 생성됐다(방 하나, 시스템
하나). 청감 평가는 진행 중이다.

## 실제로 실행한 검증

이 베타를 여는 커밋에서, macOS에서 실행한 결정론적 테스트:

- `cargo test --workspace` — 178개 통과, 하드웨어 의존 테스트 3개 무시, 0개 실패.
- `cargo clippy --workspace --all-targets -- -D warnings` — 경고 없음.
- `cargo fmt --all --check` — 통과.
- `npm test --prefix apps/desktop` — 13개 파일, 36개 테스트 통과.
- `tsc --noEmit` — 통과.

개발자의 로컬 측정 fixture가 필요한 테스트는 해당 파일이 없으면 `SKIPPED:`를 출력하고
반환한다. 따라서 새로 클론한 환경에서도 초록으로 끝나지만, 로컬 실행이 더 넓은 범위를
검증한다.

합성 48 kHz 회귀 테스트의 현재 결과:

| 지표 | Raw | 예측 |
| --- | ---: | ---: |
| 좌 20–500 Hz RMSE | 1.714 dB | 1.214 dB |
| 좌 광대역 피크 RMSE | 2.225 dB | 0.518 dB |
| 우 20–500 Hz RMSE | 1.852 dB | 1.609 dB |
| 우 광대역 피크 RMSE | 2.187 dB | 0.271 dB |

실현된 최대 필터 게인은 좌측 약 `-0.0 dB`, 우측 `+2.166 dB`이다. 후자는 합성 fixture에
의도적으로 넣은 넓고 얕은 공통 결손이 승인된 부스트 경로를 작동시킨 결과이며, +3 dB 한계
아래에 있다. 이 수치들은 합성 회귀 결과이지 실제 방에서 기대되는 성능이 아니다.

## 실제 하드웨어 증거

시스템 한 대, 개발자 한 명이다. KEF R3 Meta + KC62, WiiM Amp Ultra, UMIK-1, Roon. 이
시스템에서 실제 캡처, 분리경로 서브우퍼 탐색, 시험 필터 생성, Roon 컨볼루션, 폐루프 검증까지
완료했다. 여러 상수가 그 방 하나에서 보정되었다:

- 분리 캡처 누설 게이트의 20 dB 대역 SNR 기준(서브우퍼를 물리적으로 끈 상태에서 9.9/10.9 dB,
  켠 상태에서 32.1/33.1 dB로 측정).
- 4,800 샘플 디컨볼루션 pre-zero 창(중앙 반복 측정 일치도가 변하지 않은 0.5 ms 기준에 대해
  1.3814 ms에서 0.2394 ms로 개선).
- 옥타브 셀별 개선 게이트(측정 가능한 효과가 있던 필터가 이전의 전대역 비스무딩 판정에서
  탈락한 뒤 도입).

**방 하나로 정당화된 임계값은 상수가 아니라 가설이다.** 가장 도전해 볼 가치가 있는 숫자들이다.

## 현재 위험

- 두 타깃 프리셋은 독립적인 v1 제품 커브이며 청감 검증을 거치지 않았다. 공식 커브가 아니라는
  뜻으로 의도적으로 `-style`이라 이름 붙였다.
- 합성 임펄스 응답은 결정론적 DSP 거동을 검증할 뿐, 스피커 비선형성·잡음·마이크 클럭 드리프트·
  실제 방의 재현성을 검증하지 않는다.
- 비동기 단일 스윕 경로에는 반복된 좌우 도달 증거가 없다. 마커 클럭 매핑과 스윕 내부 ppm
  진단은 절대 채널 간 타이밍을 결정해서는 안 되며, 코드는 그렇게 할 수 없도록 구성되어 있다.
- 서브우퍼 분리경로 탐색은 예측 전용이다. 도달 시간 추정기에는 명시된 불확도가 있고(실기
  1회 실행 기준 약 ±2.75 ms), 새 결합 측정이 확인하기 전까지 그 추천은 증명된 것이 아니다.
- 권장 헤드룸은 폐루프를 통과한 실측 세션에만 존재한다. 어디에도 고정된 추정값을 대신
  넣지 않는다.
- 채널과 샘플레이트 전체에 감쇠 전용 공통 안전 정규화 하나를 기록한다. Roon 재생 중 실제
  클리핑 거동은 스트레스 테스트를 거치지 않았다.
- Roon 공식 문서는 ZIP 안의 32비트 WAV를 설명하지만, IEEE-float WAV나 추가
  `README.txt` / `manifest.json` 항목을 별도로 보장하지는 않는다. 패키지 파서는 엄격하며,
  다른 사람의 시스템에서 이루어지는 실제 Roon 임포트는 여전히 통과해야 할 관문이다.
- 지금까지 실행된 환경은 macOS뿐이다. CI는 macOS와 Windows를 정의하지만 Windows 호스트가
  실행한 적은 없다.
- macOS `.app`은 ad-hoc 서명만 되어 있다. Developer ID 서명도, 공증도 되어 있지 않다.
- 의존성 라이선스는 Cargo 메타데이터에 모두 존재하지만, 서드파티 고지 묶음 패키징과 실제
  배포 검토는 이루어지지 않았다.
- 장치 탐색과 캡처 스모크 테스트는 물리적 마이크와 OS 권한이 필요하므로 무인 CI에서 무시된
  상태로 둔다. 사용 중 장치 분리 처리도 검증되지 않았다.

